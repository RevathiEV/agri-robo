fastapi>=0.104.1
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
numpy>=1.24.0
scipy>=1.9.0
Pillow>=9.0.0
requests>=2.31.0
pyserial>=3.5
gpiozero>=2.0.1
lgpio>=0.2.2.0 ; platform_system == "Linux"
tflite-runtime>=2.14.0 ; platform_system == "Linux" and python_version < "3.12"

# TensorFlow is optional and should be installed separately only on systems
# where disease detection is needed and a supported build is available.
